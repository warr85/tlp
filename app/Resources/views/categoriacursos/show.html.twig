{% extends 'base.html.twig' %}

{% block body %}
    <h1>CategoriaCursos</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ categoriaCurso.nombre }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ categoriaCurso.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_categorias_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestion_categorias_edit', { 'id': categoriaCurso.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
