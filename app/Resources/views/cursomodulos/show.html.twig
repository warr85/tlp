{% extends 'base.html.twig' %}

{% block body %}
    <h1>CursoModulos</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ cursoModulo.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ cursoModulo.descripcion }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ cursoModulo.codigo }}</td>
            </tr>
            <tr>
                <th>Secuencia</th>
                <td>{{ cursoModulo.secuencia }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ cursoModulo.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_cursos_modulos_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestion_cursos_modulos_edit', { 'id': cursoModulo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
