{% extends 'base.html.twig' %}

{% block body %}
    <h1>Curso list</h1>

    <table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Descripcion</th>                
                <th>Id</th>
                <th>Imagename</th>
                <th>Updatedat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for curso in cursos %}
            <tr>
                <td><a href="{{ path('admin_gestion_cursos_show', { 'id': curso.id }) }}">{{ curso.nombre }}</a></td>
                <td>{{ curso.descripcion }}</td>                
                <td>{{ curso.id }}</td>
                <td>{{ curso.imageName }}</td>
                <td>{% if curso.updatedAt %}{{ curso.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_gestion_cursos_show', { 'id': curso.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_gestion_cursos_edit', { 'id': curso.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_cursos_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
