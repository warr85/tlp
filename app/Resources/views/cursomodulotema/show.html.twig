{% extends 'base_admin.html.twig' %}

{% block body %}
    <h1>CursoModuloTema</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cursoModuloTema.id }}</td>
            </tr>
            <tr>
                <th>Orden</th>
                <td>{{ cursoModuloTema.orden }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_curso_modulo_temas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestion_curso_modulo_temas_edit', { 'id': cursoModuloTema.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
