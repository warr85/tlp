{% extends 'base.html.twig' %}

{% block body %}
    <h1>CursoModuloUnidad</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ cursoModuloUnidad.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ cursoModuloUnidad.descripcion }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ cursoModuloUnidad.codigo }}</td>
            </tr>
            <tr>
                <th>Secuencia</th>
                <td>{{ cursoModuloUnidad.secuencia }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ cursoModuloUnidad.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_unidades_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestion_unidades_edit', { 'id': cursoModuloUnidad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
