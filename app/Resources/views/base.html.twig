<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}tlprgna!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{asset('assets/vendor/bootstrap/dist/css/bootstrap.min.css')}}" />
            <link rel="stylesheet" href="{{asset('assets/vendor/font-awesome/css/font-awesome.min.css')}}" />            
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body data-spy="scroll" data-offset="20" data-target=".navbar">
        
  
             <!-- Nav Menu Section -->
                <div class="logo-menu">
                    <nav class="navbar navbar-default navbar-fixed-top" role="navigation" data-spy="affix" data-offset-top="50">
                    <div class="container-fluid">
                      <!-- Brand and toggle get grouped for better mobile display -->
                      <div class="navbar-header">
                        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                          <span class="sr-only">Toggle navigation</span>
                          <span class="icon-bar"></span>
                          <span class="icon-bar"></span>
                          <span class="icon-bar"></span>
                        </button>
                          <a class="navbar-brand" href="#"><i class="fa fa-lightbulb-o"></i> tlprgn@</a>
                      </div>

                      <!-- Collect the nav links, forms, and other content for toggling -->
                      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                        <ul class="nav navbar-nav">
                            {% block enlaces %}
                                <li class="active"><a data-scroll href="#area-principal"><i class="fa fa-home"></i> Inicio</a></li>
                                <li><a data-scroll href="#servicios"><i class="fa fa-cogs"></i> Servicios</a></li>
                                <li><a data-scroll href="#cursos"><i class="fa fa-lightbulb-o"></i> Cursos</a></li>
                                <li><a data-scroll href="#pie"><i class="fa fa-info"></i> Info</a></li>                                                                        
                            {% endblock %}
                        </ul>

                        <ul class="nav navbar-nav navbar-right">
                            {% if app.user %}
                                <li><a href="#">Bienvenido {{ app.user.username }}!</a></li>
                                <li><a href="{{ path('estudiante_homepage') }}"><i class ="fa fa-power-off"></i> Área de estudiante</a> </li>
                                <li><a href="{{ path('logout') }}"><i class ="fa fa-close"></i> Cerrar Sesión</a></li>
                            {% else %}
                            <li><a href="{{ path('login') }}"><i class="fa fa-user"></i> Ingresar</a></li>
                            {% endif %}

                        </ul>
                      </div><!-- /.navbar-collapse -->
                    </div><!-- /.container-fluid -->
                  </nav>
                </div>
            
 
        
        
        {% block body %}{% endblock %}
        
        {% block javascripts %}            
            <script src="{{asset('assets/vendor/jquery/dist/jquery.min.js')}}"></script>
            <script src="{{asset('assets/vendor/bootstrap/dist/js/bootstrap.min.js')}}" type="text/javascript"></script>
        {% endblock %}
    </body>
</html>