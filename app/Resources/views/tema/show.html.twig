{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tema</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ tema.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tema.descripcion }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ tema.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_temas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestion_temas_edit', { 'id': tema.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
