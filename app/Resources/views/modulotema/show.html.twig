{% extends 'base.html.twig' %}

{% block body %}
    <h1>ModuloTema</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ moduloTema.nombre }}</td>
            </tr>
            <tr>
                <th>Orden</th>
                <td>{{ moduloTema.orden }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ moduloTema.descripcion }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ moduloTema.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_temas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestion_temas_edit', { 'id': moduloTema.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
