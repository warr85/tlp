{% extends 'base_admin.html.twig' %}

{% block body %}
    <h1>CursoModuloTemaEvaluacion</h1>

    <table>
        <tbody>
            <tr>
                <th>Ponderacion</th>
                <td>{{ cursoModuloTemaEvaluacion.ponderacion }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ cursoModuloTemaEvaluacion.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestion_tema_evaluaciones_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestion_tema_evaluaciones_edit', { 'id': cursoModuloTemaEvaluacion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
